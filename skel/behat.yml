default:
  suites:
    default:
      paths: [ features/feature ]
      contexts: [ FeatureContext ]
    # To repeat for all cake plugins
    #filter_features:
    #  paths: [ plugin/feature/ ]
    #  contexts: [ PluginContext ]